<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <title>BlooDiscounts</title>
    <!-- style CSS -->
</head>

<body>
    <div align="center">
        <h1> Administrators </h1>
        <%= form_for :session, url: login_path do |f| %>
            <div class="field">
                <%= f.label :adm_username %><br />
                <%= f.text_field :adm_username, autofocus: true %>
            </div>
            <div class="field">
                <%= f.label :password %><br />
                <%= f.password_field :password, autocomplete: "off" %>
            </div>
            <div class="actions">
                <%= f.submit "Log in" %>
            </div>
        <% end %>
    </div>
</body>
</html>