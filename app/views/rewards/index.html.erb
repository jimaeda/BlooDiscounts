
<div align="center">
  <h1> BlooDiscounts </h1>
  <h3>Resgatar recompensas</h3>
  <br/>
	<% if current_user %>
		<h3>Você tem <%= current_user.points %> pontos</h3>
	<% end %>
	<br/>
  <table style="max-width:50%" class="table table-hover">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Categoria</th>
        <th>Quantidade</th>
        <th>Custo</th>
        <th colspan="1"></th>
      </tr>
    </thead>

    <tbody>
      <% @rewards.each do |reward| %>
        <tr>
          <td><%= reward.name %></td>
          <td><%= reward.category %></td>
          <td><%= reward.quantity %></td>
          <td><%= reward.cost %></td>
          <td><%= link_to 'Retrieve', reward %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to raw("<span class='oi oi-arrow-left'> </span>Voltar"), user_profile_path, class: "btn btn-danger edit-btn-danger" %>
</div>
